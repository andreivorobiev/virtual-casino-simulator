package io.github.andreivorobiev.virtualcasino;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/**
 * Host-side smoke test retained with the generated Android project.
 */
public class ExampleUnitTest {

    @Test
    public void arithmeticTestHarnessRuns() {
        // Verify the generated project can execute its host-side JUnit harness.
        assertEquals(4, 2 + 2);
    }
}

# String Inventory Map

**Historical.** Records the planning-time string survey for the premium i18n work. The `I18N-*`
requirements below have since been registered and are no longer proposals; `docs/requirements/requirements.json`
is authoritative for their wording and status.

This inventory is based on read-only inspection of `web/app.js`, `web/admin.js`, `web/core/*.js`, `web/games/*.js`, `web/index.html`, `web/admin.html`, `tests/run_tests.py`, module manifests, and the requirement registry.

## Requirements Context

Relevant existing requirements:

- `CORE-005`: lobby loads independently of individual game modules.
- `CORE-006`: top navigation exposes all games equally.
- `CORE-015`: compact 1080p layout uses panel-level scroll areas.
- `LEDGER-025`: player balances are visible in the main UI.
- `ADMIN-013`: Admin uses a dedicated control-plane layout.
- `ADMIN-019`: Admin Audio & Voice stores global audio settings.
- `UX-001` through `UX-006`: stable game areas, action rails, result regions, scroll areas, autoplay status, and transform/opacity animation discipline.

Requirements proposed here, since registered (all three are now `PASS`; IDs and wording per
`docs/requirements/requirements.json`):

- `I18N-001`: Browser-visible UI strings are resource-backed with English default, Russian locale, and deterministic fallback.
- `I18N-002`: Switching language updates visible text without resetting route, game state, wallet state, ledger state, bot state, or autoplay state.
- `I18N-003`: Admin exposes Language/Locale controls with persisted settings.

## Shell and Lobby

Files:

- `web/index.html`
- `web/app.js`
- `web/core/ui.js`

String groups:

- Brand/title: `Virtual Casino Simulator`, validation release subtitle, fake-money-only note.
- Navigation: `Lobby`, game labels, `Admin`.
- Wallet: `Balance...`, `Balance: {amount}`, `Add fake money`.
- Lobby cards: game names, game descriptions, `Open {game}`.
- Loading/error states: `Loading...`, `Could not load {route}`, `Back to lobby`, `Could not load state: {message}`.
- Toasts: `Fake money added.`, `Auto stopped`.

Notes:

- Game descriptor labels currently live in `app.js`; move display labels/descriptions to `shell.json` or `common.json`.
- Keep route ids and module export names unchanged.
- `money()` currently uses `toLocaleString(undefined, ...)`; replace with `formatMoney()` from i18n runtime.

## Admin

Files:

- `web/admin.html`
- `web/admin.js`

String groups:

- Sidebar tabs: Dashboard, Players & Bots, Ledger, History, Telemetry, Game States, Audio & Voice, Autoplay, Requirements, Tests, System, Back to Casino.
- Page titles/subtitles for every tab.
- Dashboard cards: App, Players, Bots, Active autoplay, Errors today, Requirements.
- Table headers: Time, Player, Game, Type, Amount, Round, Before, After, ID, Name, Status, Tests, etc.
- Players/Bots controls: Enabled, strategy, Stake, Save {bot}.
- Audio controls: Master sound, SFX, Voice, volumes, preferred voice, rate, pitch, announce per game, save/preview buttons.
- Autoplay Admin controls: Sessions, Stop all autoplay.
- System/diagnostic labels: Module revisions, Raw overview.
- Proposed new Language/Locale controls.

Notes:

- Existing Admin audio settings provide the persistence pattern to mirror.
- Admin currently builds large `innerHTML` strings; extraction should use small helper functions for repeated table headers and controls.

## Shared Core Controls

Files:

- `web/core/autoplay.js`
- `web/core/bots.js`
- `web/core/voice.js`
- `web/core/api.js`

String groups:

- Autoplay: Auto play, Off, Stopping..., Running {remaining}, Speed, Slow, Medium, Fast, Rounds, Start auto, Stop, Auto play stopped.
- Bots: Bots are not enabled for this game, Bot controllers, Strategy assignment lives in Admin, Bot, Strategy, Stake, Balance, No eligible bots.
- Voice: speech utterances generated by games; move phrase templates to game resources.
- API errors: `Bad JSON from {path}`, `API error {status}`.

Notes:

- `data-testid` values in autoplay are already stable and must not be localized.
- Backend error text should remain unchanged in phase one unless an error-code mapping is explicitly added.

## Roulette

File: `web/games/roulette.js`

String groups:

- Controls: Roulette, Wheel, Single 0, Double 0, Zero rule, Normal: zero loses, La partage, En prison, Chips.
- Outside bets: Red, Black, Odd, Even, 1-18, 19-36, 1 to 18, 19 to 36, 1st 12, 2nd 12, 3rd 12, 2 to 1.
- Special bets: Racetrack & special, Snake, Voisins, Tiers, Orphelins, Jeu Zero, Number + neighbors, Final digit, Complete number, Call/final number.
- Actions: Hide/Show inside spots, Rebet, SPIN, Clear bets.
- Status/results: Sound settings moved to Admin, Last result, No spin yet, Bets debit immediately when placed, Rolled {number}, No human bets settled.
- Details: Bet slip, Total, No bets placed, Scoreboard, Last 1000 stats, Rolls, Hot, Cold, Frequency, Latest.
- Toast/speech: That bet is not available, Roulette rolled {number}.

Notes:

- Bet labels from the backend catalog should remain raw for bet placement but may need display-label mapping if catalog labels are visible.
- Roulette board labels have fixed spatial constraints. Russian labels should be short or use known abbreviations.

## Slots

File: `web/games/slots.js`

String groups:

- Controls: Slots, Active paylines, Line bet, SPIN.
- Stats/results: Progressive, Free spins, Reels, Cost, Payout, Scatter {count}, Line {index}, pays, No win, Spin to play.
- Paytable: Paytable, SCATTER, awards 8 free spins, Progressive, active line wins the meter.
- History: Recent spins, {count} lines.
- Speech: Slots paid {amount} dollars.

Notes:

- Symbol ids such as `CHERRY`, `WILD`, and `SCATTER` are game data. Display labels may be localized later, but ids must remain stable.

## Keno

File: `web/games/keno.js`

String groups:

- Controls: Keno, Ticket amount, Quick pick 5, Quick pick 10, Clear picks, Buy ticket, Draw 20.
- Board/status: Pick 1-20 spots, Selected {count}/20, Catches, Payout.
- Details: Open tickets, No ticket, Drawn balls, Paytable, Bot results, Spots, Pays by catches.
- Toast/speech: Pick at least one Keno number, Keno allows up to 20 spots, Keno caught {count} numbers.

Notes:

- Selected spots are module state and must survive language switching.

## Bingo

File: `web/games/bingo.js`

String groups:

- Controls: Bingo, Card amount, Pattern, Any line, Four corners, Postage stamp, Blackout, Buy card, Call next ball, Reset.
- Board/status: Card, Buy a card to start, Status, Calls, Last, Winner, Payout, No active session.
- Details: Called balls, Cards in play, Recent sessions, calls, none.
- Speech: Bingo ball {label}.

Notes:

- `BINGO` column letters and `FREE` square are part of the game format. Decide whether `FREE` localizes to Russian or remains the traditional board label.

## Blackjack

File: `web/games/blackjack.js`

String groups:

- Controls: Blackjack, Bet amount, Deal, Hit, Stand, Double, Split, Surrender, Insurance, Even money.
- Rules: Table rules, Decks, Blackjack payout, Dealer hits soft 17, Double after split, Late surrender, Split aces receive one card, Save rules.
- Table/status: Table, Dealer, Player hands, Hand {index}, Bet {amount}, Total, Round {id}, Deal to start.
- Details: Recent rounds, Shoe, Cards remaining, {count} hand(s).
- Toast/speech: Deal first, Blackjack rules saved, Blackjack hand settled.

Notes:

- Backend statuses and outcomes are currently displayed raw. Phase one can map known values to display keys while preserving raw API values.

## Baccarat

File: `web/games/baccarat.js`

String groups:

- Controls: Baccarat, Chips, Player, Banker, Tie, Auto repeat bet, Deal.
- Table/status: Table, Winner, Natural, yes, no, Shoe remaining, Place a bet and deal, Open bets, No open bet.
- Details: Road, Shoe, Shoe ID, Coup, Cards remaining, Burn, Scoreboard.
- Speech: Baccarat {winner} wins.

Notes:

- Player/Banker/Tie are visible bet labels and backend bet types. Translate display labels only; keep payload values `player`, `banker`, `tie`.

## Tests

File: `tests/run_tests.py`

Relevant observations:

- Browser tests rely heavily on `data-testid`; these must remain stable and untranslated.
- Some tests use visible text: `Off` and `Winner:`. Future i18n work should migrate those checks to stable test ids or locale-aware expectations.
- API tests assert endpoint behavior and should add locale settings coverage without changing existing assertions.


# Slots evidence status

Evidence class: real-backend `after_pass` acceptance evidence generated by the shared browser gate (`BR-SLOT-001`, `BR-SLOT-ECONOMICS-001`, `BR-SLOTS-PAYLINE-001`, `BR-SLOT-LINE-BET-001` and their sibling cases in `tests/run_tests.py`).

The browser lane mounts the catalog-discovered game and records self-describing PNG/JSON pairs under `logs/test-runs/` (uploaded as sharded CI artifacts). Each sidecar records branch, exact commit, surface, state, locale, viewport, path, and focused control; imagery is acceptance evidence only when its sidecar commit matches the tested head. Known-failing or manually assembled imagery is not acceptance evidence.

Generated acceptance matrix for the PR-526 presentation redesign (staggered decorative reel strips landing on the authoritative grid, drawn symbol artwork, and the premium cabinet):

| Surface | State | Locales | Viewports | Required proof |
| --- | --- | --- | --- | --- |
| `slots` | `idle` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Drawn symbol faces with non-empty caption text in every cell, seeded meter, no result, disabled repeat, no horizontal overflow |
| `slots` | `spinning` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Exactly 15 `.slots-symbol.spinning` cells, disabled spin action, stable cabinet and result boxes within 2 px, decorative strip overlay riding the live cell geometry |
| `slots` | `win` / `multi_win` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | All twenty labelled payline paths within 1 CSS px of live cell centers, twenty distinguishable stroke/dash styles, visible symbol identity under `elementFromPoint`, exact engine payout copy |
| `slots` | `bonus` / `repeat_available` / `route_restored` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Feature drawer, trusted paid-trigger basis, restored jackpot identity and payline set after remount |
| `slots` | `reduced_motion` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Zero spinning cells at idle, static payline paths with `animation-name: none` and zero transition duration, bounded layout |
| `slots` | `zoomed` | en-US | desktop primary | 125 percent zoom geometry realignment within 1 CSS px |

Deterministic non-browser proof of the new reel behavior lives in `tests/games/slots/test_frontend.mjs` (authoritative-row landing and seamless launch composition per column, per-column travel growth, 140-240 ms adjacent stop stagger with final-reel-only anticipation, first-four-reels scatter tease detection, governed comfort/autoplay hold budgets, disposed-scope timer guards, and mid-spin repaint deferral).

Scope note: this packet evidences the compatibility-bounded presentation slice only. The `SLOT-030..035` full motion-adoption gate (Slow/Medium/Fast stop profiles, 100-spin mapped acceptance, video evidence with independent review) remains PLANNED and is not claimed by this evidence.

# Fan-Tan lifecycle-adopter evidence

This after-pass evidence covers the fifth bounded adopter slice of issue #718 and permanent owners `CORE-034`, `TEST-248`, `FAN-TAN-004`, and `FAN-TAN-005`. The affected visual-matrix surface is `fan_tan`; the slice preserves its `ready`, `counting`, `settled`, `reduced_motion`, `route_restored`, and `repeat_available` presentation while moving route, busy, locale, and stylesheet ownership into `web/core/game_lifecycle.js` and the same CSS declarations into a formatted external module asset.

## Evidence

`issue-1001-after-pass-desktop-en-US.png` is captured by the permanent `BR-FAN-TAN-001` Browser case from the real authenticated route outlet before its first real settled round. The case requires exactly one `link#fan-tan-styles` pointing to `/games/fan_tan.css`, the unchanged three-column desktop route with a dominant counting stage, real settlement, reduced-motion presentation, reload-safe history, and a real repeated wager.

The accepted PNG is 405,254 bytes with SHA-256 `50ed76e96bdfe08db95a2520c44c42707f625eb74616f1841902ffed35284861`. It shows the English `ready` state at the primary desktop viewport. The same case retains existing after-pass captures for both locales, all four governed viewports, and every matrix state; no intentional visual delta, game math, wager, payout, outcome, ledger, API, contract, provider, package, release, or deployment change is part of this slice.

## Acceptance boundary

The listener-free `UI-GAME-LIFECYCLE-001` case owns lifecycle semantics and duplicate-helper deletion. The dedicated `BR-FAN-TAN-001` case owns the rendered external-style proof and real game flow. Fan-Tan's cryptographic `ft-` action identity and ambiguous-response replay remain game-owned and unchanged. Future issue #718 adopter slices repeat this boundary per game rather than broadening earlier adopters.

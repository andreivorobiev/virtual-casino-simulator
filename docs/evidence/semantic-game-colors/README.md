# Semantic game-color evidence

This evidence surface covers the bounded `UX-024` application-style foundation and its sole permanent Browser owner, `BR-GAME-COLOR-001` (`UX-024`, `TEST-149`). It does not change or qualify game math, wagers, payouts, outcomes, economy, ledger, APIs, providers, public policy, package, release, or deployment behavior.

## Hosted exact-head matrix

The Browser case mounts four real catalog routes and captures exactly 32 after-pass cells: four games × English/Russian × `desktop_primary`, `desktop_compact`, `tablet`, and `mobile`. Every PNG has a JSON sidecar that binds the tested branch and commit and records the bounded computed-style receipt.

| Visual-matrix surface | State | Rendered semantic proof |
| --- | --- | --- |
| `roulette` | `semantic_game_colors` | Real table-pocket red and green plus the shared metallic-gold token |
| `color_wheel` | `semantic_game_colors` | Rendered red, green, and gold bet-control gradients |
| `marble_race` | `semantic_game_colors` | Rendered red, green, and gold/yellow marble fills |
| `keno` | `semantic_game_colors` | A real selected-number metallic-gold state plus the shared felt-green catch token |

Every cell also computes the actual shared brand-mark gradient and an off-screen production-cascade playing-card suit probe. The case requires the existing rose brand chrome, red suit foreground, and ivory card face to remain unchanged, removes its temporary probe before capture, and restores English, the primary desktop viewport, and the lobby route.

## Acceptance boundary

Only `web/styles.css` changes product source. The Browser case imports no contributor game-source or module-version hunk, performs no real-money or outcome mutation, and produces no local Browser or heavy-QA claim. Issues #74 and #554 remain open; #481 precedes held #518, every other merge remains serialized, and #450 is excluded.

# Roulette evidence status

Evidence class: real-backend `after_pass` acceptance evidence generated by the shared browser gate (`BR-ROU-PREMIUM-001`, `BR-ROU-001`, `BR-ROU-MOTION-CURVE-001`, `BR-ROU-REDUCED-MOTION-001`, `BR-I18N-GAMESTATE-ROU-001` and their sibling cases in `tests/run_tests.py`).

`ROU-072` extends the existing permanent `BR-ROU-REDUCED-MOTION-001` owner case with the real sixteen-cell normal/reduced action matrix; it does not allocate a new Browser case ID.

The browser lane mounts the catalog-discovered game and records self-describing PNG/JSON pairs under `logs/test-runs/` (uploaded as sharded CI artifacts). Each sidecar records branch, exact commit, surface, state, locale, viewport, path, and focused control; imagery is acceptance evidence only when its sidecar commit matches the tested head. Known-failing or manually assembled imagery is not acceptance evidence.

Generated acceptance matrix for the PR-526 presentation redesign (honest-landing wrappers around the tracked ROU-069/ROU-070 compatibility curves, plus the layered wheel and felt visuals):

| Surface | State | Locales | Viewports | Required proof |
| --- | --- | --- | --- | --- |
| `roulette` | normal-motion `idle` / `betting` / `wagered` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Layered wheel art, parked ball without a fake pocket (ROU-051), aligned chips, localized controls, and no horizontal overflow |
| `roulette` | normal-motion `spinning` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | A real public spin mounts the rotor/ball curves and honest-landing wrapper transitions; progress copy and locked controls remain visible without layout drift |
| `roulette` | normal-motion `settled` / `route_restored` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | Ball remains captured in the authoritative backend pocket, the table/history/result identities agree, and remount preserves the same settled presentation |
| `roulette` | reduced-motion `spinning` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | A real public spin remains visibly in progress while both motion channels and every honest-landing wrapper transition are suppressed by the real media preference |

Deterministic non-browser proof of the new landing behavior lives in `tests/games/roulette/test_frontend.mjs` (pocket congruence on both wheel modes, off-wheel rejection, whole-turn opposite-direction wrapper travel, latency-floor travel budget, seeded per-round determinism, API-pending and timer-pending disposal, exactly-one settle/abort completion, no stale DOM/wallet/sound/bot/refund continuation, clean remount, and the issue-246 unmount refund suppression). The static motion-profile contract remains pinned by `tests/roulette_motion_tests.py`.

Hosted acceptance is complete only when exact-head sidecars capture the real in-progress normal and reduced action branches for every one of the sixteen locale/viewport/motion combinations above: two locales × four governed viewports × normal and reduced motion. A static class toggle, primary-desktop-only artifact, or sidecar from another commit is insufficient.

Scope note: this packet evidences the compatibility-bounded presentation slice only. The `ROU-063..068` Authentic/Quick pacing gate (15-18 s reveals, 16-plus ball circuits, video evidence with independent review) remains PLANNED and is not claimed by this evidence.

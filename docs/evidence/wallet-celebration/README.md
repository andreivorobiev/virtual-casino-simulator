# Wallet celebration evidence status

Evidence class: exact-head `after_pass` acceptance evidence generated inside the existing permanent `BR-LOBBY-001` Browser case in `tests/run_tests.py`. `UX-023` does not allocate a new Browser case ID or change the Browser shard inventory.

The Browser lane uses the visible registered-user wallet and the real current-user fake-token credit route to execute the changed application controller. It records PNG/JSON pairs under `logs/test-runs/`, and hosted Browser shards upload them as source-bound artifacts. Each sidecar records the tested branch, exact commit, `shell_lobby` surface, state, locale, viewport, path, and focused control. An image is acceptance evidence only when its sidecar commit equals the qualified head; locally assembled, stale-head, known-failing, or static class-toggle imagery is not acceptance evidence.

Required exact-head matrix:

| Surface | State | Locales | Viewports | Required proof |
| --- | --- | --- | --- | --- |
| `shell_lobby` | `wallet_gain` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | A real 25-token authoritative gain leaves the exact latest balance visible, mounts one bounded positive-delta chip and ordinary glow, preserves containment, and cleans every transient after completion. |
| `shell_lobby` | `wallet_big_win` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | A real 250-token authoritative gain leaves the exact latest balance visible, mounts one chip and one layer containing exactly twelve decorative coins, preserves containment, and cleans every transient after completion. |
| `shell_lobby` | `wallet_gain_reduced_motion` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | The same real ordinary gain settles the exact balance immediately with no marker, animation class, chip, coin layer, or delayed cleanup. |
| `shell_lobby` | `wallet_big_win_reduced_motion` | en-US, ru-RU | desktop primary, desktop compact, tablet, mobile | The same real threshold-level gain settles the exact balance immediately with no marker, animation class, chip, coin layer, or delayed cleanup. |

The complete gate is thirty-two cells: two installed locales times four governed viewports times two gain magnitudes times normal or reduced motion. The test must execute the real balance update in every cell and fails if reduced motion accidentally follows the normal branch. It restores English, the primary desktop viewport, normal motion, and a clean wallet before downstream Browser cases.

Listener-free lifecycle proof lives in `tests/pwa_tests.py`. It executes the real `web/core/celebrate.js` module with deterministic document, timer, randomness, motion-preference, and page-lifecycle seams. The proof covers an initial silent seed; exactly-once ordinary gain, large gain, and loss settlement; bounded coins; overlapping authoritative updates; hostile replay of canceled callbacks; navigation interruption; pagehide/session disposal; reduced motion; disposed-generation rejection; and clean remount. The controller creates no `MutationObserver` or animation-frame loop and never rewrites the wallet after the application settles the latest server value.

Scope note: this evidence is shared-shell presentation only. It changes no game result, wager, payout, wallet arithmetic, ledger event, API or contract, provider, public policy, PWA version, release, or deployment behavior. Issue #74 remains open, and held #481 and #518 must reconcile `web/styles.css` after this lane.
